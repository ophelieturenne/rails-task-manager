<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to "#{task.title} #{task.completed}", task_path(task)%></li>
  <% end %>
</ul>

<%= link_to "+ Add a new task", task_new_path %>
